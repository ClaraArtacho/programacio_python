#RO_Pag180_ex8.py

def sim(x):
    f = 0
    a = x // 1000
    b = (x - (a * 1000)) // 100
    c = (x - (a * 1000)-(b * 100)) // 10
    d = x%10
    if 
    
    
    return(f)
    mirem si els primers num son igual a zero
    i despres igualem  els elements per veure si con simètrics
    